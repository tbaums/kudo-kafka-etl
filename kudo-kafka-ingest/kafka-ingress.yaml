apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: traefik
  name: kafka-ingress
spec:
  rules:
  - http:
      paths:
      - path: /kafka-ep
        backend:
          serviceName: kafka-svc
          servicePort: 9093
