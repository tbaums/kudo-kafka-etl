---
apiVersion: v1
kind: Service
metadata:
  name: kafka-ingest-ui-svc
spec:
  selector:
    app: kafka-ingest
  ports:
    - port: 80
      targetPort: 80
---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: traefik
  name: kafka-ingest-ui-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        backend:
          serviceName: kafka-ingest-ui-svc
          servicePort: 80
